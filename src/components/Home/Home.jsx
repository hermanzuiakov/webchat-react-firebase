import React from 'react';
import Sidebar from "../Sidebar/Sidebar";

const Home = () => {
    return (
        <main className={"w-full h-screen"}>
            <Sidebar />
        </main>
    );
};

export default Home;